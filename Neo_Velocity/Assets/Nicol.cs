using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Nicol : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Console.Log(); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
